# ==============================================================================
# Linux test bootstrap - lightweight test-only deps.
# Mirrors the version constraints from requirements.txt but excludes the
# heavy UI/voice stack (tkinter, pywebview, tensorflow, piper-tts,
# faster-whisper, openwakeword, sounddevice, soundfile).
# Goal: get pytest + the core graph/data modules importable on a Linux venv.
# ==============================================================================

# Core graph / LLM stack (no actual LLM calls in unit tests, but importable)
pytest>=8.0
pydantic>=2.0.0
langchain-core>=0.2.0
langchain-community>=0.2.0
langchain-ollama>=0.1.0
langchain-openai>=0.1.0
langchain-text-splitters>=0.2.0

# Async backend + network
fastapi>=0.110.0
uvicorn>=0.28.0
httpx>=0.27.0
requests>=2.31.0

# Data parsing
numpy>=1.26.0
pyyaml>=6.0
beautifulsoup4>=4.12.0
lxml>=4.9.0
python-multipart>=0.0.9

# RAG document parsing
pypdf>=4.0.0
python-docx>=1.1.0
