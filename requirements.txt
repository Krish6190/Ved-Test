 # ==============================================================================
   # 1. CORE GRAPH ARCHITECTURE, LLM AND DATA PARSING ENGINES
   # ==============================================================================
   langgraph>=0.1.0
   langchain-core>=0.2.0
   langchain-community>=0.2.0
   langchain-ollama>=0.1.0
langchain-openai>=0.1.0  # OpenRouter cloud routing (USE_CLOUD_API=true)
   langchain-text-splitters>=0.2.0
   pyyaml>=6.0
   pydantic>=2.0.0
   beautifulsoup4>=4.12.0
   lxml>=4.9.0
   numpy>=1.26.0

   # ==============================================================================
   # 2. AUDIO PIPELINE (WHISPER VOICE INPUT / PIPER TTS OUTPUT)
   # ==============================================================================
   sounddevice>=0.4.6
   soundfile>=0.12.1
   faster-whisper>=1.0.0
   openwakeword>=0.6.0
   piper-tts>=1.2.0
   # openwakeword needs a TFLite runtime. The standalone tflite-runtime package
   # only ships wheels for Python <=3.11, and openwakeword's import error
   # misleadingly tells users to install it even when tensorflow is present.
   # tensorflow (>=2.16 on Windows) ships tf.lite.Interpreter, which
   # openwakeword detects automatically. Use tensorflow as the universal
   # TFLite runtime so the project works on 3.11, 3.12, and beyond.
   tensorflow>=2.16.0

   # ==============================================================================
   # 3. NEW ASYNCHRONOUS BACKEND AND CORE NETWORK INTEGRATIONS
   # ==============================================================================
   requests>=2.31.0
   fastapi>=0.110.0
   uvicorn>=0.28.0
   httpx>=0.27.0

   # ==============================================================================
   # 4. LIGHTWEIGHT NATIVE UI DESKTOP CONTAINER WINDOW
   # ==============================================================================
   pywebview>=5.0

   # ==============================================================================
   # 5. TESTING
   # ==============================================================================
   pytest>=8.0

   # ==============================================================================
   # 6. RAG DOCUMENT PARSING (PDF + DOCX support for file uploads)
   # ==============================================================================
   pypdf>=4.0.0
   python-docx>=1.1.0

# ==============================================================================
# 7. HTTP FORM / FILE UPLOAD PARSING (required by FastAPI's UploadFile)
# ==============================================================================
python-multipart>=0.0.9
